import { Col, Row } from "react-bootstrap";

function Header (props) {
    return <>
        <Row>
            <Col>Logo</Col>
            <Col>Home</Col>
            <Col>Menu</Col>
        </Row>
    </>;
}

export default Header;